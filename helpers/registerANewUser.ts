
export const registerANewUser=async()=>{
    return process.env.NEWUSER = `${process.env.NAME_CATEGORY}` + Math.floor(Math.random() * 100) +'@gmail.com' //Create a random user to be able to register a user from the first time x number of time that we want

}